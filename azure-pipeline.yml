steps:
  - script: echo Hello!
    condition: always()
  - task: AzureResourceGroupDeployment@2
    inputs:
      azureSubscription: "MySPNConnection"
      action: "Create"
      resourceGroupName: "rg-matsd-t02"
      location: "westeurope"